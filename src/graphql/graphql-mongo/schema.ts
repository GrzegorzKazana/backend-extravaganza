// stealing schema from 'schema-first', could move it to common,
// but it feels weird for 'schema-first' to not have schema inside
import schema from '@/graphql/schema-first/schema';
export default schema;
